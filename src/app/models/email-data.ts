import { NgModule } from "@angular/core";

export interface EmailData {
  contactName: string;
  email: string;
  nameplate: string;
  motorInfo: string;
  assembly: string;
  application: string;
  additionalInfo: string;
}
